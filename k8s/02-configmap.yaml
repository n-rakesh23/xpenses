apiVersion: v1
kind: ConfigMap
metadata:
  name: xpense-config
  namespace: xpense
data:
  NODE_ENV: "production"
  PORT: "3000"
  # Replace with your actual domain — used for CORS and magic-link emails
  APP_URL: "https://xpense-rakesh.duckdns.org"
  API_URL: "https://xpense-rakesh.duckdns.org"
