apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: xpense-ingress
  namespace: xpense
  annotations:
    # Remove the cert-manager annotation if you are not using cert-manager for TLS
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - yourdomain.com         # Replace with your actual domain
      secretName: xpense-tls
  rules:
    - host: yourdomain.com       # Replace with your actual domain
      http:
        paths:
          # Backend: all /api/* and /webhook/* traffic
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 3000
          - path: /webhook
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 3000
          # Health check path for load-balancer probes
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 3000
          # Frontend: everything else
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
